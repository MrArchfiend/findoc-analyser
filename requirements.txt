# ── Core RAG pipeline ─────────────────────────────────────────────────────────
groq>=0.9.0                  # LLM inference (question generation + response generation)
pypdf>=3.0.0                 # PDF text extraction
chromadb>=0.4.0              # Vector store (embeddings + similarity search)
sentence-transformers>=2.2.0 # Local embedding model (all-MiniLM-L6-v2)
nltk>=3.8.0                  # Sentence tokenisation for chunking

# ── Document format support ───────────────────────────────────────────────────
python-docx>=0.8.11          # DOCX text extraction

# ── OCR fallback (optional — only needed for scanned/image-based PDFs) ────────
pdf2image>=1.16.0            # Converts PDF pages to images for OCR
pytesseract>=0.3.10          # Tesseract OCR wrapper
# Note: Tesseract binary must also be installed separately:
#   Ubuntu/Debian: sudo apt install tesseract-ocr
#   macOS:         brew install tesseract
#   Windows:       https://github.com/UB-Mannheim/tesseract/wiki

# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.100.0             # Web framework
uvicorn[standard]>=0.23.0   # ASGI server
python-multipart>=0.0.6     # Required for FastAPI file uploads
slowapi>=0.1.7               # Rate limiting middleware for FastAPI

# ── Monitoring & evaluation ───────────────────────────────────────────────────
ragas>=0.1.0                 # RAG evaluation framework (faithfulness, relevancy, etc.)
langchain-groq>=0.1.0        # Groq LangChain integration (RAGAS judge LLM)
langchain-community>=0.0.38  # HuggingFaceEmbeddings for RAGAS
datasets>=2.14.0             # Required by RAGAS for Dataset.from_dict()
