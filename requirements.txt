# ── Core RAG pipeline ─────────────────────────────────────────────────────────
groq>=1.1.1                  # LLM inference (question generation + response generation)
pypdf>=6.9.1                 # PDF text extraction
chromadb>=1.5.5              # Vector store (embeddings + similarity search)
sentence-transformers>=5.3.0 # Local embedding model (all-MiniLM-L6-v2)
nltk>=3.9.3                  # Sentence tokenisation for chunking

# ── Document format support ───────────────────────────────────────────────────
python-docx>=1.2.0           # DOCX text extraction

# ── OCR fallback (optional — only needed for scanned/image-based PDFs) ────────
pdf2image>=1.17.0            # Converts PDF pages to images for OCR
pytesseract>=0.3.13          # Tesseract OCR wrapper
# Note: Tesseract binary must also be installed separately:
#   Ubuntu/Debian: sudo apt install tesseract-ocr
#   macOS:         brew install tesseract
#   Windows:       https://github.com/UB-Mannheim/tesseract/wiki

# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.115.0             # Web framework
uvicorn[standard]>=0.34.0   # ASGI server
python-multipart>=0.0.20    # Required for FastAPI file uploads
slowapi>=0.1.9               # Rate limiting middleware for FastAPI

# ── Monitoring & evaluation ───────────────────────────────────────────────────
ragas>=0.1.21                # RAG evaluation framework (faithfulness, relevancy, etc.)
langchain-groq>=0.1.9        # Groq LangChain integration (RAGAS judge LLM)
langchain-community>=0.2.0   # HuggingFaceEmbeddings for RAGAS
datasets>=2.20.0             # Required by RAGAS for Dataset.from_dict()